/* const fs = require("fs");
const path = require("path");
const mv = require("mv");

const oldLocation = path.join(__dirname, "src", "styles.css");
const newLocation = path.join(__dirname, "public", "styles.css");

fs.rename(oldLocation, newLocation, function (err) {
    if (err) {
        throw err
    } else {
        console.log("Successfully moved the file!");
    }
});
*/
